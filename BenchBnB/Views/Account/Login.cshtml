@model BenchBnB.Models.ViewModels.Login

@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm())
{
    @Html.ValidationSummary()
    
    <div class="form-group row">
        <div class="col-xs-3 ml-3">
            @Html.LabelFor(m => m.Email)
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
        </div>
    </div>

    <div class="form-group row">
        <div class="col-xs-3 ml-3">
            @Html.LabelFor(m => m.Password)
            @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
        </div>
    </div>

    <div>
        <button type="submit" class="btn btn-success btn-lg">Login</button>
        @Html.ActionLink("Cancel", "Register", "Account", null, new { @class = "btn btn-secondary btn-lg" })
    </div>
}
<br />
<p>@Html.ActionLink("Don't have an account? Click here to register now!", "Register")</p>

<script src="~/Scripts/buttonToggler.js"></script>